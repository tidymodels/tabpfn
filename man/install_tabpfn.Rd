% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{install_tabpfn}
\alias{install_tabpfn}
\title{Install the TabPFN Python environment}
\usage{
install_tabpfn(
  version = "default",
  envname = "r-tabpfn",
  check_latest = TRUE,
  extra_packages = NULL,
  python_version = NULL,
  method = c("auto", "virtualenv", "conda"),
  new_env = identical(envname, "r-tabpfn"),
  restart_session = TRUE,
  ...
)
}
\arguments{
\item{version}{The \code{tabpfn} version to install. Use \code{"default"} (or \code{NULL})
to install the latest release, a bare version string such as \code{"2.0.9"} to
pin an exact version, or a full pip specification such as \code{">=2.0"}.}

\item{envname}{The name of the Python virtual environment to create or use.
The default, \code{"r-tabpfn"}, is auto-discovered by reticulate.}

\item{check_latest}{A logical. When \code{TRUE} (the default) and no explicit
\code{version} is given, an existing environment is compared against the latest
release on PyPI and you are asked whether to upgrade. Set to \code{FALSE} to
skip this check (useful when intentionally staying on an older version).}

\item{extra_packages}{An optional character vector of additional Python
packages to install alongside \code{tabpfn}.}

\item{python_version}{An optional Python version to use for the environment.}

\item{method}{The installation method, passed to \code{\link[reticulate:py_install]{reticulate::py_install()}}.}

\item{new_env}{A logical. When \code{TRUE}, an existing environment named
\code{envname} is removed and rebuilt from scratch. Defaults to \code{TRUE} only for
the canonical \code{"r-tabpfn"} environment.}

\item{restart_session}{A logical. When \code{TRUE} (the default) and running in
RStudio, the R session is restarted after installation so the new
environment takes effect.}

\item{...}{Additional arguments passed to \code{\link[reticulate:py_install]{reticulate::py_install()}}.}
}
\value{
Invisibly returns the environment name.
}
\description{
Sets up a persistent Python virtual environment containing the \code{tabpfn}
Python library so that \code{\link[=tab_pfn]{tab_pfn()}} and friends can use it. By default the
environment is named \code{"r-tabpfn"}, which reticulate automatically discovers
and prefers over an ephemeral environment (see the \emph{Environment discovery}
section).
}
\section{Environment discovery}{

Because the package calls \code{reticulate::import("tabpfn")}, reticulate will
automatically use a virtual environment named \code{"r-tabpfn"} if one exists,
preferring it over the ephemeral environment that is otherwise created on
demand. Environments selected via \code{RETICULATE_PYTHON}, \code{VIRTUAL_ENV}, or a
project-local \code{.venv} take precedence over \code{"r-tabpfn"}.
}

\examples{
\dontrun{
# Install the latest release into "r-tabpfn"
install_tabpfn()

# Pin a specific version
install_tabpfn(version = "2.0.9")
}
}
