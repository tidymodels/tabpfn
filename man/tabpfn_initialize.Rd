% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzz.R
\name{tabpfn_initialize}
\alias{tabpfn_initialize}
\title{Eagerly initialize the TabPFN Python library}
\usage{
tabpfn_initialize()
}
\value{
\code{NULL}, invisibly. Called for its side effect of loading the Python
library.
}
\description{
Forces the Python \code{tabpfn} library (and its PyTorch dependency) to load now
instead of on first use. Because PyTorch bundles its own OpenMP runtime,
loading it before any other package that uses OpenMP avoids the segmentation
fault described in \url{https://github.com/tidymodels/tabpfn/issues/34}.

For this to work, call it as the very first thing in your session, using
\code{tabpfn::tabpfn_initialize()} (with the \code{::} prefix so it runs before
\code{library(tabpfn)} and before any other package that might load OpenMP, such
as \pkg{recipes}):

\if{html}{\out{<div class="sourceCode r">}}\preformatted{tabpfn::tabpfn_initialize()
library(tabpfn)
suppressPackageStartupMessages(library(recipes))
fit_obj <- tab_pfn(mpg ~ ., data = mtcars)
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
tabpfn::tabpfn_initialize()
library(tabpfn)
}
}
